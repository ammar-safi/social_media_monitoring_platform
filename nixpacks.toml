[phases.setup]
nixPkgs = ["php82", "composer" , "nodejs_20"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader","php artisan key:generate --force || true"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"