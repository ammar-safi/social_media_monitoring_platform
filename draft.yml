models:
    # User:
    #     id: id
    #     first_name: string
    #     last_name: string
    #     user_name: string
    #     password: string
    #     email: string unique
    #     phone_number: string unique
    #     type: enum:"admin","gov_off","policy_maker" default:"gov_off"
    #     active: boolean default:"0"
    ApprovesUser:
        id: id
        admin_id: bigInteger foreign:users.id
        user_id: bigInteger foreign:users.id
    Invite:
        id: id
        cov_off_id: bigInteger foreign:users.id
        policy_maker_id: bigInteger foreign:users.id
        email: string
        new_column: bigInteger
    ApprovesPolicyMaker:
        id: id
        admin_id: bigInteger foreign:users.id
        policy_maker_id: bigInteger foreign:users.id
        expired_at: timestamp
    Post:
        id: id
        content: text
        platform: string nullable
        account: string nullable
        gov_org_id: bigInteger foreign:gov_org.id
    GovOrg:
        id: id
        name: string
        email: string nullable
    Analyst:
        id: id
        post_id: bigInteger foreign:posts.id
        sentiment: enum:"positive","negative","normal" nullable
        stance: enum:"supportive","against" nullable
    Hashtag:
        id: id
        name: string
    HashtagPost:
        id: id
        hashtag_id: bigInteger foreign:hashtags.id
        post_id: bigInteger foreign:posts.id
